This folder contains the server and client configuration files.
These files must have the ".properties" extension and contain some arguments to configure the client and the server.
They will be read from the project at runtime and used as configuration.
They are identical at the moment, but it's important to distinguish them for clarity and possible future implementations.
