The gson lib is a Google's library to work with .json files.
It's used to load and store the data handled by the project on .json files.
The users' data and the orders' data are stored with it.

The JLine lib is used to customize the terminal's behaviour to better handle the user's input in the client's CLI.

