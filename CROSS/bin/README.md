All the project's source files are inside the package "cross", divided in subfolders by area.
To use it in the code (the public ones), an import example could be cross.server.Server.

So, e.g.:

import cross.server.Server;
// Other code...
